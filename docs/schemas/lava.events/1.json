{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://lavaphp.dev/schemas/lava.events/1.json",
  "title": "lava events --json",
  "description": "Every event app/Listeners.php names and the listeners it reaches, in the order they run: the registry as boot read it, so a listener that is not listed here does not run. Registered by lavaphp/events.",
  "allOf": [
    { "$ref": "https://lavaphp.dev/schemas/lava-envelope/1.json#/$defs/envelope" },
    {
      "properties": {
        "schema": { "const": "lava.events/1" },
        "command": { "const": "events" },
        "data": {
          "type": "object",
          "required": ["file", "events"],
          "properties": {
            "file": {
              "description": "app/Listeners.php when the app has one; null when it has none.",
              "anyOf": [{ "const": "app/Listeners.php" }, { "type": "null" }]
            },
            "events": {
              "type": "array",
              "description": "One entry per key of app/Listeners.php, in file order. An event reaches the listeners of every entry it is an instance of, each listener once.",
              "items": {
                "type": "object",
                "required": ["event", "listeners"],
                "properties": {
                  "event": {
                    "type": "string",
                    "description": "An event class, or an interface the events it covers implement."
                  },
                  "listeners": {
                    "type": "array",
                    "minItems": 1,
                    "description": "Container ids of the listeners, in the order they run.",
                    "items": { "type": "string" }
                  }
                },
                "additionalProperties": false
              }
            }
          },
          "additionalProperties": false
        }
      }
    }
  ]
}
